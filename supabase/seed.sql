-- Stable reference rows live in reviewed migrations so remote environments and
-- local resets remain identical. Add disposable local-only fixtures here, but
-- never seed protected cookbook text, scans, franchise artwork, credentials, or
-- production user data.

select 1;
